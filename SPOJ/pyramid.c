#include <stdio.h>
#include <math.h>
int main() {
	// your code here
	double u1,v1,w1,u,v,w,U,V,W,volume,a,b,c,d,e;
	//scanf("%d %d %d %d %d %d",&u,&v,&w,&U,&V,&W);
	//printf("%d %d %d %d %d %d \n",u,v,w,U,V,W );
	scanf("%d",&v);
	printf("%d\n",(v+1));
	/*u1=(v*v)+(w*w)-(U*U);
	v1=(w*w)+(u*u)-(V*V);
	w1=(u*u)+(v*v)-(W*W);
	printf("%d %d %d\n",u1,v1,w1 );
	a=(u*u);
	printf("%d\n",a );
	b=u*u*u1*u1;
	c=v*v*v1*v1;
	d=w*w*w1*w1;
	e=u1*v1*w1;
	printf("%d %d %d %d %d\n",a,b,c,d,e );
	//volume = (1⁄12) * sqrt((4*(u*u*v*v*w*w)) - (u*u*u1*u1) - (v*v*v1*v1) - (w*w*w1*w1) + (u1*v1*w1));
	//printf("%d\n",volume);*/

	return 0;
}